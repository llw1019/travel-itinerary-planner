# travel-itinerary-planner

差旅／旅遊行程規劃的 Claude skill。**clone 這個 repo、在裡面開 Claude Code,就可以直接開始排行程** —— skill 放在 `.claude/skills/travel-itinerary-planner/`,Claude Code 會自動載入。

使用者說「幫我排三天東京」「幫老闆排一趟出差」「把這份行程做成手冊」「幫我核對這份行程」都會觸發它。

## 這個 repo 是什麼

`.claude/skills/travel-itinerary-planner/` 同時是兩件事:

1. **可直接使用的 Claude Code skill**(clone 就能用)
2. **要打包上傳到 claude.ai 的 skill 原始碼**

只有一份,沒有副本,所以不會有兩邊不同步的問題。

## skill 支援兩個執行環境

SKILL.md 的工具名稱以 claude.ai 聊天介面為準,檔案開頭有一張**環境對應表**,列出在 Claude Code 要換成哪些工具(`AskUserQuestion`、`WebSearch`、`Read`/`Write`、本地檔案交付、`profiles/` 等)。流程與所有品質標準兩邊完全相同,只有工具名稱不同。

編修時的注意事項:

- **不要因為某個工具在這個工作區不存在,就把它當成錯誤刪掉。** `places_search`、`weather_fetch`、`present_files`、`/mnt/skills/...` 都是 claude.ai 專屬,它們是對的
- **這一條的用途是擋住錯誤的編輯決定,不是拿來當開場白。** 使用者問 zip 是不是最新的、要求改某一條規則、或直接叫 skill 排行程,就做那件事 —— **不要在回覆開頭放一段環境說明**
- 反過來,**新增任何用到工具的規則時,要同時在環境對應表補上另一個環境的做法**,或明確寫出該環境沒有這個能力、要怎麼降級
- Claude Code 少兩個能力:沒有地點查詢(拿不到 Google 評分與 `place_id`)、沒有天氣工具。兩者都用 `WebSearch` 補,查不到就照原規則換一個查得到的
- **問卷需要互動 session。** 非互動執行(`-p`、排程、CI)跑不了問卷

## 檔案分工

| 檔案 | 職責 | skill 執行時何時讀 |
|---|---|---|
| `SKILL.md` | 環境對應表、三種入口(A／B1／B2)、流程 Step 0–10 | 永遠載入 |
| `references/intake-questions.md` | 問卷題庫 A–H、A-2 事由分支、設定檔儲存格式 | Step 2、Step 3 |
| `references/research-and-scheduling.md` | 資料查證規則、查證筆記、排程七步、限制回核、飯店為主的日子 | Step 4,排每一天之前 |
| `references/html-output-spec.md` | 14 個必含區塊的欄位規格、兩環境交付流程、階段一 + 兩種加工(圖片／深度介紹)+ 兩種衍生版(B5 列印手冊／A4 精簡版) | Step 6、Step 9、Step 10 |
| `references/editorial-standards.md` | 編輯判斷、推薦理由寫法、用字與專有名詞 | Step 6,寫內容時 |
| `references/output-checklist.md` | 硬擋 17 項 + 完整驗收清單(266 項,交付詳細版實跑 193 項) | Step 7,交付前 |

## 執行時產生的資料存哪裡

skill 跑起來會產生兩種要留下來的東西。**兩者都放在專案根目錄下、使用者看得到的地方,而且都被 `.gitignore` 排除 —— repo 裡只有乾淨的 skill 原始碼,不含任何個人資料。**

| 資料 | 位置 | 內容 |
|---|---|---|
| 行程手冊 | `itineraries/` | 產出的 HTML,含訂房編號、聯絡電話、完整費用 |
| 手冊的圖片 | `itineraries/<與 html 同名>/pics/` | 使用者自己提供的圖片;經同意才補的自由授權圖放 `pics/cc/`(含 `_licences.json` 授權記錄) |
| 旅遊設定檔 | `profiles/<名稱>.md` | 各對象的差旅偏好(艙等、步調、預算、用餐時間等) |

這些目錄都由 skill 在需要時自行建立。**不要把設定檔寫進 Claude Code 的記憶目錄**(`~/.claude/projects/.../memory/`)—— 那裡使用者看不到也改不動,而且按專案分、不好備份。

新增任何會落地存檔的資料類型時,照同一個原則:放在專案根目錄下可見的位置,加進 `.gitignore`,並在交付訊息告訴使用者路徑。

## 編修守則

**規則的權威來源是 reference 檔,不是 SKILL.md。** SKILL.md 只重述歷史上最常被違反的少數規則,其餘一律以 reference 檔為準。改規則要改 reference,再回頭檢查有沒有別處重述同一條 —— 已知的重述清單在 `README.md`。

**這個 repo 歷史上重複發生過的錯誤是「改一端忘了另一端」。四種,全部發生過:**

1. 加或改**輸出要求**(spec、editorial)之後,回頭檢查 intake 有沒有收集到那個資訊。否則會出現「輸出承諾了問卷沒問的東西」。
2. 加或改**輸入題目**(intake)之後,檢查輸出端有沒有落腳處、排程階段會不會真的用到。否則會出現「問卷收集了但完全沒用上」。
3. 加新的**入口或情境**之後,回頭補 frontmatter `description` 的觸發詞(上限 1024,目前約 985)。否則寫了流程但 skill 根本進不來。
4. **在 reference 改了規則之後,回頭改 SKILL.md 裡重述它的那段摘要。** 這是**發生頻率最高的一種** —— 一次 review 抓到的 13 個問題裡有 7 個是它。SKILL.md 永遠載入而 reference 未必當下會讀,所以摘要過時的殺傷力比 reference 本身有錯還大:它會讓人照著一份看起來完整、其實少了一條的清單做事。

   高風險的摘要有這幾段,改到對應的 reference 就要一起看:
   - Step 4 的**排程七步摘要**與三條硬規則 ← `research-and-scheduling.md`
   - Step 6 的**14 個必含區塊列舉** ← `html-output-spec.md`
   - Step 9 的**加圖十步**與深度介紹七點 ← `html-output-spec.md`
   - Step 10 的 **B5 編排順序**與 **A4 不能砍的幾項** ← `html-output-spec.md`
   - 開頭的**參考檔案索引**(每個檔一句話講它管什麼)← 全部
   - `README.md` 的**規則重述同步表** ← 全部

其他約定:

- **插入新章節時要重新編號,不要用「三之二」這種寫法。** 這個偷懶已經犯過三次(`三之二`、`一之二`、又一次 `三之二`),每次都要再花一個 commit 修。插進去就順手把後面的編號往後推
- 跨檔引用一律用**描述性寫法**(「見編輯標準的主題附錄一節」「見同步鏈的最後兩點」),不要寫「第五節」「第 9 點」—— 編號一調整就過時,**已經發生三次**。最近一次是同步鏈插入一點之後,寫死「第 9 點」的地方指錯了對象,而同一條規則在另一個檔用「最後兩點」的寫法就沒壞
- 加規則之前先想清楚它會不會讓問卷輪次或驗收清單膨脹到被跳著看。題庫已 ~80 項、清單已 266 項,**再加東西要同時說明怎麼維持可用**
- 檔案一律 UTF-8 無 BOM

## 打包上傳到 claude.ai

```bash
python -c "
import zipfile, os
src = '.claude/skills/travel-itinerary-planner'
with zipfile.ZipFile('travel-itinerary-planner.zip','w',zipfile.ZIP_DEFLATED) as z:
    for r,d,fs in os.walk(src):
        for f in sorted(fs):
            p = os.path.join(r,f)
            z.write(p, os.path.relpath(p, '.claude/skills').replace(os.sep,'/'))
"
```

zip 內的路徑必須是 `travel-itinerary-planner/SKILL.md`(資料夾在最上層)。**不要用 Windows PowerShell 5.1 的 `Compress-Archive`** —— 它把路徑分隔符寫成反斜線,部分解壓工具會把整個路徑當成一個檔名,`references/` 子目錄結構會丟失。

## 已否決的方向(不要再提)

- **主行程每個時段並列 2-3 個候選** → 已改為「單一推薦定案 + 備案獨立成章並標明可替換哪一段」。當初的問題不是「一個 vs 三個」,是「單一定案卻沒有備案」
- **一次產出詳細版與衍生版** → 已改為漸進式。內容還在改的階段維護多份,改一個時間要改好幾個檔案。衍生版(B5 列印手冊、A4 精簡版)一律事後、**逐一分開問**、經同意才做,而且從定案的詳細版重新生成,不獨立編輯
- **把「要不要做 B5 列印手冊」與「要不要做 A4 精簡版」併成一個「要不要做衍生版」的問題** → 兩份不會同時需要(可能只要一種、可能圖片的取捨相反),合併會逼使用者一次決定兩件事。要分開問
- **設定檔自動套用** → 已改為逐項詢問式套用
- **只在 B 類提醒「同行者需求可能不同」** → 已提升為 B–H 全類別通用原則
- **放一份填了假資料的範例 HTML 當品質基準** → 會把捏造的班號店名寫進 skill,違反 skill 自己的規則;只放佔位符的空模板又無法示範密度且帶來樣板感。改用「單一時間軸項目的完整範例」+ 列印 CSS 片段
- **未經同意就把行程手冊發布成 artifact** → 交付**預設是本地檔案**:`Write` 出完整 standalone HTML(含 `<meta charset="utf-8">`、字體不外連)存到工作目錄的 `itineraries/`,回覆給絕對路徑。**不要存到 session 的 scratchpad**(會被清掉)。要不要另外發布成 artifact **必須先問**,同意才發 —— 沉默不等於同意。行程裡有訂房編號、聯絡人電話、費用,不該預設幫使用者放上可分享的網址。
- **在 repo 加 `.claude/settings.json` 權限允許清單** → 使用者明確決定不加。skill 會做大量 `WebSearch`/`WebFetch`,clone 的人第一次跑會遇到權限提示,那是刻意的:不替不認識這個 repo 的人預設任何權限。**不要好心加回去。**
- **主動去找圖、或熱連外部圖片** → **不主動找,也一律不熱連**(離線失效、多半禁止 hotlink)。但**不等於不能找**:缺圖的版位可以**提議**用明確標示自由授權的圖片補(Wikimedia CC / 公有領域),**列出打算用哪幾張、哪個版位、什麼授權,經同意才下載內嵌**,並在圖說標作者與授權、記進 `pics/cc/_licences.json`。沒同意就只給缺圖清單,並把那個版位改成不需要圖的版式
- **從「資料夾裡有什麼」開始排圖** → 已改為**需求先於供給**。而且**不是每張提供的圖都該進正式版位** —— 純資訊截圖(訂房單、費用明細)轉成文字欄位、清單型照片(代購、藥妝)收成附錄的採買清單表、地圖與站牌照放在動線旁邊
- **給圖片張數訂上限或常數**(「詳細版 10 張」「B5 20–30 張」) → 提過又推翻。**張數是「一個物件 1–2 張、有特色的才要」套上去的結果,跟行程長度連動、沒有上限** —— 兩天三四張、三十天幾十張。寫死常數在長行程一定錯,而且它是從一張「每格都要填數字」的需求表逼出來的。**不要再做那張表**
- **把主體分布(橫向帶狀／縱向延伸／居中物件)預存成 `<img>` 的 `data-` 屬性** → 判準是「這個值能不能被直接吃掉」:**裁切重心可以** —— 寫成 inline `object-position`,詳細版無效、B5 加 `object-fit:cover` 自動生效;**主體分布不行** —— 它的用途是「挑哪一個版位」,存了到時候還是要有人讀了再去選。一律留到做 B5 那個 session 開圖判斷
- **把深度介紹的圖併進加圖那一輪** → 深度介紹是**獨立的一件事**,自己走完一輪(挑對象 → 查證 → 寫 → 為這幾則單獨要圖)。**為它多跟使用者要一次圖是正常的**,不要為了省一輪而少做,也不要因此規定「先做深度介紹再加圖」的順序

## 現況

紙上 review 已收斂。**skill 從未實跑過完整一輪。** 三個懷疑點只有實測才驗得出來:

1. 266 項的驗收清單會不會還是被跳著看
2. 排程七步會不會被壓縮成「憑感覺排,然後回頭補理由」
3. 限制回核會不會變成打勾儀式,沒有真的逐項對

使用者曾提供兩份上海行程 PDF 當品質底標(未進 git 的 `ref/`),內容已吸收進 `editorial-standards.md`,那個資料夾可以移除,不需連帶改任何檔案。
